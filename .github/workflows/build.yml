name: Android CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8


    - name: Setup Android SDK
      uses: android-actions/setup-android@v2

    - name: Build with Script
      run: |
        echo "Staring build"
        echo "Android sdk location: [${ANDROID_SDK}] or possibly [${ANDROID_SDK_ROOT}]"
        ./build.sh -p
